# Docker Compose Environment Variables
# Used for volume naming and project identification

# Project name for volume naming (prevents conflicts with other projects)
COMPOSE_PROJECT_NAME=gareth-passbolt

# Database Configuration
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=passbolt
MYSQL_USER=passbolt
MYSQL_PASSWORD=P4ssb0lt

# Passbolt Configuration
APP_FULL_BASE_URL=https://passbolt.local/example
EMAIL_DEFAULT_FROM=admin@passbolt.com

# SSL/TLS Configuration
PASSBOLT_SSL_FORCE=true

# Valkey Configuration
VALKEY_PORT=6379
